# See https://fly.io/docs/reference/configuration/
app = "voice-soundboard-mcp"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile.mcp"

[env]
  PORT = "8080"
  VOICE_SOUNDBOARD_BACKEND = "mock"
  NODE_ENV = "production"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[[http_service.checks]]
  grace_period = "10s"
  interval = "30s"
  method = "GET"
  path = "/health"
  timeout = "5s"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
